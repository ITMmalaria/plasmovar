{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/pmoris/plasmovar/main/assets/schema_input.json",
    "title": "pmoris/plasmovar pipeline - params.input schema",
    "description": "Schema for the file provided with params.input",
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "sample": {
                "type": "string",
                "pattern": "^\\S+$",
                "errorMessage": "Sample name must be provided and cannot contain spaces (multiple samples can share the same name if they can be distinguished by lane or library)",
                "meta": ["id"]
            },
            "lane": {
                "type": ["string", "integer", "null"],
                "pattern": "^L?\\d{1,3}$",
                "errorMessage": "Lane number must be provided if samples are split across lanes and cannot be distinguished. Lane identifier cannot contain spaces",
                "meta": ["lane"],
                "default": null
            },
            "library": {
                "type": ["string", "integer", "null"],
                "pattern": "^\\S+$",
                "errorMessage": "Library identifier is required to distinguish between samples with the same name (and potentially lane) if multiple libraries were sequenced.",
                "meta": ["library"],
                "default": null
            },
            "fastq_1": {
                "type": "string",
                "format": "file-path",
                "exists": true,
                "pattern": "^([\\S\\s]*\\/)?[^\\s\\/]+\\.f(ast)?q\\.gz$",
                "errorMessage": "FastQ file for reads 1 must be provided, cannot contain spaces and must have extension '.fq.gz' or '.fastq.gz'"
            },
            "fastq_2": {
                "type": "string",
                "format": "file-path",
                "exists": true,
                "pattern": "^([\\S\\s]*\\/)?[^\\s\\/]+\\.f(ast)?q\\.gz$",
                "errorMessage": "FastQ file for reads 2 cannot contain spaces and must have extension '.fq.gz' or '.fastq.gz'",
                "default": null
            }
        },
        "required": ["sample", "fastq_1"]
    }
}
